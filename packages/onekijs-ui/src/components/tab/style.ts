import { css } from 'styled-components';
import { ComponentStyle } from '../../styles/typings';
import { TabsProps } from './typings';

export const tabsStyle: ComponentStyle<TabsProps> = ({ animate = 550, theme }) => {
  const t = theme.tabs;
  return css``;
};
