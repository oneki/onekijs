import styled from 'styled-components';
import SliderComponent from './components/SliderComponent';
import { sliderStyle } from './style';

const Resizer = styled(SliderComponent)`
  ${sliderStyle}
`;

export default Resizer;
